{% extends 'base.html.twig' %}

{% block title %}Mon panier - Mon P'tit Matin{% endblock %}

{% block content %}

<div class="mb-5 ms-4 mt-4">
    <h1>Mon panier</h1>
    Retrouvez l'ensemble des produits que vous avez ajouté à votre panier.    
</div> 
{{ dump(cart) }}   
    <div class="container">
      <div class="d-flex justify-content-center">
        <table class="table mx-auto">
          <thead class="table-dark">
            <tr class="text-center">
              <th scope="col">Produits</th>
              <th scope="col"></th>
              <th scope="col">Quantité</th>
              <th scope="col">Prix</th>
              <th scope="col">Total</th>
              <th scope="col"></th>
            </tr>
          </thead>
          <tbody>
            {# {% set total = null %} 
            {% set compteur = 0 %}
            {% for formule in cart %} 
            <pre>
              {{ dump(cart) }}
            </pre>          
            <tr>
              <td class="text-center"><img src="/uploads/{{ formule.formule.illustration }}" alt="{{ formule.formule.title}}" height="75px"></td>
              <td>
                {{ formule.formule.title }} <br><i>(
                  {% for produit in formule.product %}
                  {{ produit.name }} x {{ formule.productQuantity }},
                  {% endfor %}  )</i>
              </td>
              <td class="text-center">{{ formule.quantity }}</td>
              <td class="text-center">{{ (formule.formule.price / 100) |number_format(2, ',', '.') }} €</td>
              <td class="text-center">{{ ((formule.formule.price * formule.quantity) / 100) |number_format(2, ',', '.') }} €</td>
              <td>
                <a href="{{ path('app_delete_to_cart', { 'orderId' : formule.orderId }) }}">
                  <img src="{{ asset('assets/img/supprimer.png')}}" height="18px" alt="Supprimer mon produit">
                </a>
              </td>
            </tr>
              {% set total = total + (formule.formule.price * formule.quantity) %}
            {% endfor %} #}
          </tbody>
        </table>
      </div>
   
      <div class="d-flex mt-3">
        <div class="col-md-7">
          
        </div>
        <div class="col-md-5">
          <div class="text-end">
              <b>Nombre de formule :</b> {{ cart|length }}<br>
              <b>Livraison :</b> 3 €<br>
              {# <b>Total de mon panier :</b> {{ ((total / 100)+3) |number_format(2, ',', '.') }} €  #}
              <a href="" class="btn btn-dark w-100 mt-3">Valider mon panier</a>
          </div>
        </div>
      </div>
    </div>

{% endblock %}

{# {{ path('app_delete_to_cart', { 'deleteOrderId' :  }) }} #}
{# {{ path('app_delete_to_cart', { 'orderId' : deleteOrderId }) }} #}
{# {{ path('app_delete_to_cart', { 'orderId' : delete.orderId}) }} #}